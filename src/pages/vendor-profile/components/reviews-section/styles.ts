import { Box, styled } from "@mui/material";

const StyledReviewsSection = styled(Box)`
	align-item: flex-start;
	display: flex;
	flex-direction: column;
	gap: 24px;
`;

const StyledReviewsContent = styled(Box)`
	align-item: flex-start;
	display: flex;
	flex-direction: column;
	gap: 12px;
`;

export { StyledReviewsContent, StyledReviewsSection };
